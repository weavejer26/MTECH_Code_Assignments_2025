//In Class Assignments Functions JW 10/27/2025 
{
    function greet(name) { //Only Console.log
        console.log("Hello " + name);
    }

    greet("Jeremy");

    function addNumber(a, b) { //Add Numbers
        return a + b;
    }

    console.log(addNumber(5, 10));

    function multNumber(a, b) { //Multiply Numbers
        return a * b;
    }

    console.log(multNumber(5, 10));

    function twoParams(a, b) { //Two parameters
        return a - b;
    }

    console.log(twoParams(20, 15));

    function threeParams(a, b, c) {
        return a + b + c;
    }

    console.log(threeParams(1, 2, 3));

    function divideAdd(a, b, c) { // Add and Divide
        return (a + b) / c;
    }

    console.log(divideAdd(6, 4, 5));
}
